<div>
	<div class="s-card {{$color}}">
		<i class="{{$icon}}"></i>
		@if($subtitle)<div class="title">{{$subtitle}}</div>@endif
		@if($title)<div class="total number">{{$title}}</div>@endif
		@if($footer)<div class="bot text-center">{{$footer}}</div>@endif
	</div>
</div>